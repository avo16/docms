<% provide(:title, 'All users') %>
<html>
	<head>
		<title>Docms | <%= yield(:title) %> </title>
	</head>
	<h1>All users</h1>

	<ul class="users">
		<% @users.each do |user| %>
		<li>
			<%= link_to user.name, user %>
		</li>
	<% end %>
	</ul>
</html>